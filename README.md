# Demo of Leaflet and Vue

This is a very basic demo of a Vue.js single-file component containing a
Leaflet.js map. Everything is bundled by Rollup.js.

```bash
git clone
npm install
npx rollup --config
```